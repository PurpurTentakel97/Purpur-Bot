def identifier_for_db(string: str) -> str:
    return string.strip().lower()


def id_for_db(string_id: str) -> str:
    return string_id.strip()


def name_for_db(string: str) -> str:
    return string.strip().title()
